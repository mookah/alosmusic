version: "3.8"

services:
  rtmp:
    image: alfg/nginx-rtmp:latest
    container_name: alos_rtmp
    ports:
      - "1935:1935"
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./hls:/opt/data/hls
    restart: unless-stopped